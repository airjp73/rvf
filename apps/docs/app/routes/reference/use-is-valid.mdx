---
meta:
  title: useIsValid (Remix Validated Form)
---

import { PropHeader } from "~/components/PropHeader";

<PropHeader
  variant="h1"
  prop="useIsValid"
  type="(formId?: string) => boolean"
/>

A hook that returns whether or not the current form valid.

The `formId` parameter allows you to [use the hook outside a form](/use-outside-forms).
