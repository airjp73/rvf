---
meta:
  title: Server validation (Remix Validated Form)
---

import { Footer } from "~/components/Footer";
import { CodeExample } from "~/components/CodeExample";
import Demo, { loader } from "~/examples/defaultValues";

export { loader };

# Default values

The `defaultValues` prop on `ValidatedForm`
makes it easy to set default values for the entire form all in one place.

<CodeExample>
  <Demo />

```tsx file=~/examples/defaultValues.tsx

```

</CodeExample>

---

<Footer
  prev={{
    to: "/server-validation",
    label: "Server Validation",
  }}
  next={{
    to: "/validation-library-support",
    label: "Validation Library Support",
  }}
/>
