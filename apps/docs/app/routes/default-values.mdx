---
meta:
  title: Server validation (Remix Validated Form)
---

import { CodeExample } from "~/components/CodeExample";
import Demo from "~/examples/defaultValues";

# Default values

The `defaultValues` prop on `ValidatedForm`
makes it easy to set default values for the entire form all in one place.

<CodeExample>
  <Demo />

```tsx file=~/examples/defaultValues.tsx

```

</CodeExample>
